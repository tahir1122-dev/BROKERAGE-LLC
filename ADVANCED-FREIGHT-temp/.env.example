# Copy this file to .env and fill with real values to enable SMTP email delivery
# Example using Gmail SMTP (use app password if you have 2FA enabled)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your.email@gmail.com
SMTP_PASS=bqav krmk kypw rjdw

SMTP_FROM="Delivery Detective" <no-reply@yourdomain.com>

# Optional: override recipient for received agreements
AGREEMENT_RECIPIENT=tm0038763@gmail.com

# Allow cross-origin requests from your frontend dev server
ALLOW_ORIGIN=http://localhost:5173 || http://localhost:8080
